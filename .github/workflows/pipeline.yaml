name: ci

on:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
      - synchronize

permissions:
  contents: write
  id-token: write

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ──────────────────────────────────────────────
  # Always-run jobs (PR + main)
  # ──────────────────────────────────────────────
  node-audit:
    uses: platform-mesh/.github/.github/workflows/job-node-audit.yml@main
    secrets: inherit

  build:
    needs: [node-audit]
    uses: platform-mesh/.github/.github/workflows/job-node-test.yml@main
    secrets: inherit

  # ──────────────────────────────────────────────
  # Quality gate (aggregates required checks)
  # ──────────────────────────────────────────────
  quality-gate:
    if: always()
    permissions: {}
    needs: [node-audit, build]
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Check results
        run: |
          if [[ "${{ needs.node-audit.result }}" != "success" ]]; then
            echo "node-audit failed"
            exit 1
          fi
          if [[ "${{ needs.build.result }}" != "success" ]]; then
            echo "build failed"
            exit 1
          fi

  # ──────────────────────────────────────────────
  # Release jobs (main branch only)
  # ──────────────────────────────────────────────
  create-version:
    if: github.ref == 'refs/heads/main'
    uses: platform-mesh/.github/.github/workflows/job-create-version.yml@main
    secrets: inherit

  publish:
    if: github.ref == 'refs/heads/main'
    needs: [create-version, build]
    uses: platform-mesh/.github/.github/workflows/job-node-publish.yml@main
    secrets: inherit
    with:
      version: ${{ needs.create-version.outputs.strictVersion }}
      environment: npmjs:@platform-mesh/portal-server-lib
